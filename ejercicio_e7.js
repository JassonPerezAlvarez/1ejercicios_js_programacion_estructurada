// ejercico 1
let agrearElemento = (arreglo, elementos) => {
arreglo.push(elementos);
return arreglo;
}

let numeros = [1, 2, 3];

console .log(agrearElemento(numeros, 8));

// ejercicios2
let agrearElementoInicio = (arreglo, elementos ) => {
    arreglo.unshift(elementos);
     let numeros2 = 
}

// jercicio3
let eliminarElementoFinal = (arreglo) => {
    let ultimo = arreglo.pop();

}


let frutas = ['manzana', 'banana', 'naranja'];
console.log(eliminarElementoFinal(frutas)); 